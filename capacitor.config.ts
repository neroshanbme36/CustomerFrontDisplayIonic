import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'customer-facing-display-ionic',
  webDir: 'www'
};

export default config;
