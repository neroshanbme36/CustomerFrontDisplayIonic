import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yumarone.customerfacingdisplay',
  appName: 'Customer Facing Display',
  webDir: 'www'
};

export default config;
